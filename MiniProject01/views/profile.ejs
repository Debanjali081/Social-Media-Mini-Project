<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Mini Project By The Sher</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end ">
            <a class="bg-red-500 px-3 py-2 rounded-md text-sm" href="/logout">Logout</a>
        </div>

        <h3 class="text-3xl mb-5 "><span class="font-bold text-red-400">Hello</span>, <%= user.name %> 👋</h3>
        <h5>Create a new post</h5>
        <form action="/post" method="post">
            <textarea class=" w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 px-2 py-2 mt-2 rounded-md block "
            type="text" name="content" placeholder="What's on your mind?"></textarea>
            
            <!-- Updated name attribute to 'content' -->
            <input class="px-3 py-2 bg-blue-500 rounded-md block mt-2 w-40" type="submit" value="Create new post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-xl text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-4">
                <% user.posts.reverse().forEach((post) => { %>
                <div class="post w-1/3 p-4 border-2 border-zinc-700 rounded-md mb-2">
                    <h4 class="text-blue-500"><%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p> <!-- Updated to show post content -->
                    <small class="mt-1 inline-block"><%=post.likes.length%></small>
                    <div class="btns flex justify-between mt-5">
                        <a class="text-blue-500" href="/like/<%=post._id%>">
                            <%=post.likes.indexOf(user._id)=== -1 ? "Like👍" : "Unlike"%>
                        </a>
                        <a class="text-zinc-400" href="/edit/<%=post._id%>">Edit</a>
                        <a class="text-red-500" href="/delete/<%= post._id %>" onclick="return confirm('Are you sure you want to delete this post?');">Delete</a>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>

</html>