<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Mini Project By The Sher</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end ">
            <a class="bg-red-500 px-3 py-2 rounded-md text-sm" href="/logout">Logout</a>
        </div>
        <h5>Edit Your Post </h5>
        <form action="/update/<%=post._id%>" method="post">
            <textarea
                class=" w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 px-2 py-2 mt-2 rounded-md block "
                type="text" name="content" placeholder="What's on your mind?"><%=post.content%>
            </textarea>

            <input class="px-3 py-2 bg-yellow-500 text-black rounded-md block mt-2 w-40" type="submit"
                value="Update Post">
        </form>
    </div>
</body>

</html>